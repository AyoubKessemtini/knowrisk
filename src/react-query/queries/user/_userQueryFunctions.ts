const getMe = async () => {
  //Replace with Get Me usecase
  /*  const result = await core.getConversations.execute();
  return defaultQueryResult(result); */
};

export const UserQueryFunctions = {
  getMe,
};
